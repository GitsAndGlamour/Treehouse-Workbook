document.writeln("");
var answer = prompt("What programming language is the name of a gem?");
if (answer.toUpperCase() === 'RUBY' || answer.toLowerCase() === 'ruby' || answer === 'Ruby' ) {
    document.writeln("That's right!");
}
else {
    document.writeln("That's not right..")
}
